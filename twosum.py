def array_pair(arr, target):
  if len(ar) = 0 or len(arr) = 1
    return None
  for i in arr               #(O(n))
    paired = tareget - i
    if parired in arr        #(O(1))
      return (paired, i)
